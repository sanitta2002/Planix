import { SubscriptionGuardGuard } from './subscription-guard.guard';

describe('SubscriptionGuardGuard', () => {
  it('should be defined', () => {
    expect(new SubscriptionGuardGuard()).toBeDefined();
  });
});
